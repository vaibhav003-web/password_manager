# password_manager
A secure CLI-based password manager built in Python. Uses SHA-256 hashing for master password authentication and Fernet symmetric encryption to securely store credentials. Supports adding, retrieving, deleting, and displaying website passwords stored in an encrypted vault.
